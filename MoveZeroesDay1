Did my fifth problem on day 1
Move zeroes to the end:
got the intuition and replaced with non zeroes in the beginning and then added the 0s at the back
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int j=0;
        
        for(int i=0;i<nums.size();i++){
        if(nums[i]!=0){
        nums[j]=nums[i];
        j++;
}
        }
        for(int i=j;i<nums.size();i++){
            nums[i]=0;
        }
    
    }
};
