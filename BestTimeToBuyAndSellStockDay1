8th on day 1
Best time to buy and sell stock
calculated the max value from the diff of min and the current value and min value for mini and return maxi
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int maxi=INT_MIN;
        int mini=prices[0];
        for(int i=0;i<prices.size();i++){
    maxi=max(maxi,prices[i]-mini);
            mini=min(prices[i],mini);
        }
        return maxi;
    }
};
