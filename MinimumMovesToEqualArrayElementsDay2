First problem of Day 2
Got the intuition that the difference b/w the middle element and the lower and upper elements gives the answer after sorting (Yay solved it in 10 seconds)
class Solution {
public:
    int minMoves2(vector<int>& nums) {
        int ans=0;
       sort(nums.begin(),nums.end());
        int mid=(0+nums.size())/2;
        for(int i=0;i<nums.size();i++){
            ans+=abs(nums[i]-nums[mid]);
            
        }
        return ans;
    }
};
