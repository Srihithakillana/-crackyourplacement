4rth prob on day 5
To merge two linked lists 
Here a dummy is used
ListNode dummy(0); creates a dummy node with a value of 0. This dummy node helps to simplify the process of merging by providing a non-null starting point.
ListNode* current = &dummy; sets the current pointer to point to the dummy node.
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
        ListNode dummy(0);
        ListNode* current=&dummy;
        while(l1!=nullptr&&l2!=nullptr){
            if(l1->val<=l2->val){
                current->next=l1;
                l1=l1->next;
            }
            else{
                current->next=l2;
                l2=l2->next;
            }
            current=current->next;
        }
        if(l1!=nullptr){
current->next=l1;
        }
        else{
current->next=l2;}
        return dummy.next;
    }
};
